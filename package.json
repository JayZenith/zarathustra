{
  "name": "zarathustra",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript autonomous research-agent runtime",
  "type": "module",
  "bin": {
    "zarathustra": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "check": "bunx tsc --noEmit -p tsconfig.json",
    "start": "bun run src/cli/index.ts",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "^1.3.10",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.2"
  }
}
